# Manifests

Release-time manifests belong here. At minimum:

- `pdbsol_split_ids.csv`;
- `external_split_ids.csv`;
- `artifacts.json`;
- `SHA256SUMS`.

Do not add raw PDB files, upstream model weights, or private server paths.
